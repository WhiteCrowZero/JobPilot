可以。现在这版我建议正式定义成 **JobPilot v1.0 秋招作品收口计划**。

我重新核了你上传的 `JobPilot(2).zip`。有一个很重要的判断：

> **这不是“10 天重新开发 JobPilot”，而是“10 天重构主链路 + 补两个关键能力 + 清理项目叙事”。**

现有代码已经比你主观感觉完整很多：用户/收藏/目标岗位/UserSkill/Match/Knowledge/Question/StudyTask 都有；当前测试文件 33
个、测试函数约 158 个；CI、Dockerfile、PostgreSQL/Redis/RabbitMQ Compose、health/readiness 也已经存在。真正缺口集中在 **认证
Session、真实数据源、LLM Skill Extraction、MQ 你本人掌握、完整容器运行态和作品化 README**。

我建议这 10 天冻结需求，不允许再扩。

---

# 一、先冻结 JobPilot v1.0 最终故事

最终只讲这一条：

```text
3 个国内招聘数据源
        ↓
Crawler Producer
        ↓
RawJobCollectedMessage V1
        ↓
RabbitMQ
        ↓
Celery Worker
        ↓
Source Adapter
        ↓
RawJobRecord
        ↓
Normalize + Fingerprint
        ↓
JobPost / JobPostDetail
        ↓
       COMMIT
        ↓
LLM SkillExtractor
        ↓
SkillAlias / Skill Dictionary
        ↓
JobPostSkill
        ↓
用户选择 Target Job
        ↓
维护 UserSkill
        ↓
matched / weak / missing
        ↓
生成 StudyTask
        ↓
答题 / 学习进度
```

外围：

```text
JWT + UserSession
Redis
PostgreSQL
Nginx
Docker Compose
pytest
CI
```

**JobPilot v1.0 到此结束。**

---

# 二、你刚追加的“中国化”调整，我赞成，而且 Day 1 就做

你原来确实为了国际化设计了不少目前没有业务价值的东西。

现在三个新爬虫既然都面向你真正会使用的国内招聘网站，就不要背历史包袱。

我建议删掉：

```text
has_visa_sponsorship
has_relocation_support
raw_country_name
visa / relocation normalization
```

`is_remote`、`workplace_type` 我也倾向于**一起砍掉**，前提是你的三个真实来源确实没有稳定、重要的办公方式字段。

最终地点保持：

```text
locations: str | None
```

够了。

国内校招项目完全可以：

```text
北京 / 上海 / 深圳
杭州
北京 / 杭州
```

不需要：

```text
country
province
city
district
timezone
remote
hybrid
visa
relocation
```

## 保留这些岗位核心字段

例如：

```text
title
company_name
locations

employment_type
experience_level
experience_min_years
experience_max_years
education_level

salary_min
salary_max
salary_currency
salary_period

published_at
expired_at

description
source_url

fingerprint
source_id
raw_record_id
```

具体可再根据三个真实网站实际字段微调。

原则：

> **数据库根据真实数据源设计，不根据想象中的全球招聘平台设计。**

这次不要为了“以后可能用”留一堆空字段。

---

# 三、当前已经至少完成什么

这一点一定要看清楚，否则你会错误地认为自己还有 80% 没写。

## 已经可以视为基础完成

### 1. 基础工程结构

已有：

```text
FastAPI
Pydantic v2
SQLAlchemy async
Alembic
PostgreSQL
Redis
RabbitMQ
Celery
pytest
ruff
pyright
GitHub Actions
Dockerfile
```

而且目录已经按：

```text
router
schema
contract
service
repository
model
```

组织起来了。

这个不重构。

---

### 2. 用户基本认证

实际上当前代码已经有：

```text
email + password 注册
phone + password 注册
email 登录
phone 登录
Access Token
Refresh Token
Refresh 单次消费
Logout
当前用户认证
密码哈希
用户状态
is_superuser 字段
```

所以不是“只有一个登录”。

但是：

> phone 只是电话号码 + 密码，并没有短信验证。

而且真正缺的是：

> **UserSession / 多设备会话生命周期。**

---

### 3. 岗位主数据

已经有：

```text
RawJobRecord
JobSource
JobPost
JobPostDetail
Adapter
Normalization
Fingerprint
raw_content_hash
message_id
岗位列表
岗位详情
筛选
Redis filter options
```

这一大块不要重做。

---

### 4. Skill 数据体系

已经有：

```text
Skill
SkillAlias
JobPostSkill
RawSkillCandidate
Alias normalization
Skill sync
```

这套非常值得保留。

缺的是：

> **RawSkillCandidate 从哪里产生。**

之前靠招聘网站直接给 `skills`，现在改成：

```text
JD → LLM → RawSkillCandidate
```

---

### 5. 用户业务其实已经大量存在

已有：

```text
收藏
收藏夹
目标岗位
UserSkill
matched / weak / missing
KnowledgePoint
Question
StudyTask
从 Target 生成 StudyTask
作答
跳过
进度
```

所以这部分**绝对不要推翻重写**。

10 天里主要做：

> 联调 + 简化规则 + 修 bug + 演示。

不是重新设计“智能学习平台”。

---

### 6. MQ 已经有骨架

已有：

```text
独立 simulator
V1 message contract
job.import_raw
job.ingestion queue
Celery Worker
late ACK
prefetch=1
worker lost requeue
有限 retry
指数退避
两阶段事务
skill sync 状态
trace_id
message_id
MQ 联调文档
```

这部分：

> **代码存在 ≠ 你已经完成。**

因为你本人没 review。

所以在 v1.0 Definition of Done 里，必须重新算成：

> **待你验收。**

这是非常重要的区别。

---

# 四、10 天内明确不完成什么

这一部分比“做什么”还重要。

## Auth 不做

```text
OAuth
GitHub 登录
微信登录
Google 登录
短信验证码
邮箱验证码
找回密码
复杂 RBAC
管理员后台
权限管理系统
```

只做：

> password + JWT + UserSession。

---

## AI 不做

```text
Agent
RAG
MCP
Multi-Agent
向量数据库
Embedding
Rerank
自动生成学习计划的大模型 Agent
LLM 自动评估用户技能等级
```

AI 只有一个职责：

> **JD 技能结构化抽取。**

---

## MQ 不做

```text
Kafka
Exactly Once
Saga
Outbox
复杂 Exchange topology
多 Worker pipeline
独立 skill_sync queue
RabbitMQ 集群
完整 DLQ 管理后台
自动消息重放平台
```

一个：

```text
job.import_raw
```

足够。

---

## 爬虫不做

```text
Scrapy 分布式框架
代理池
验证码破解
复杂反爬
Cookie 池
浏览器自动化平台
爬虫管理后台
```

你的三个爬虫：

> **能稳定拿数据并发送 Message 就结束。**

---

## 部署不做

```text
Kubernetes
云原生平台
Service Mesh
Terraform
Prometheus + Grafana 大套件
ELK
多机部署
自动扩缩容
```

只做到：

```text
Docker Compose
Nginx
API
Worker
PostgreSQL
Redis
RabbitMQ
```

---

# 五、你和 AI 的分工原则

这次必须改变开发方式。

不是：

> 你说一句 → AI 整个模块写完。

而应该：

```text
你定义
↓
AI实现
↓
你Review
↓
你运行
↓
你制造故障
↓
你解释
```

## 必须由你本人完成的东西

### 你负责“决策”

包括：

* 三个爬虫选什么网站。
* 每个来源抓哪些字段。
* Message Contract 含义。
* Adapter 为什么存在。
* 岗位哪些字段保留。
* UserSession 生命周期。
* 什么错误 retry。
* 什么错误不 retry。
* SkillExtractor 输入输出。
* Skill Alias 的职责。
* matched / weak / missing 规则。
* StudyTask 为什么这么生成。

这些以后都是面试问题。

---

## 你负责“关键代码”

至少亲手写：

### 三个 crawler

你自己已经决定这一点，非常好。

### 每个来源 Adapter 的核心字段映射

例如：

```python
title = ...
company_name = ...
location = ...
description = ...
external_job_id = ...
```

我建议你自己写。

Adapter 本身不难，但非常能体现：

> 你有没有真正理解数据边界。

### MQ 主链路 review

不要求全部重写。

但：

```text
producer
contract
task routing
task
transaction
retry
idempotency
```

你必须逐行搞懂。

### LLM structured output schema

先由你定义：

> 我到底希望模型返回什么？

AI 再帮你实现 client/provider。

---

## AI 适合干什么

大量机械工作交给 AI：

* Alembic migration。
* 删除 visa/remote 等字段后的 repository/schema/test 同步修改。
* Session repository CRUD。
* API schema。
* Docker Compose。
* Nginx 配置。
* LLM Provider client boilerplate。
* mock client。
* 测试脚手架。
* 重复测试补齐。
* README 整理。
* 架构图 Mermaid。
* 静态类型报错修复。
* Ruff formatting。
* 帮你 code review。
* 模拟面试追问。

这样才叫：

> **AI-Native Development**

而不是：

> AI 替你开发项目。

---

# 六、正式 10 天规划

这里假设：

> 工作日 2～3 小时左右
> 周末能集中投入更多。

如果实习特别忙，可以变成 12 天，但**不要改变顺序**。

---

# Day 1 —— Scope Freeze + 中国化数据模型

## 目标

把项目从“全球求职平台设想”正式改成：

> **国内技术岗位情报 + 求职准备系统。**

### 你做

第一件事，不写代码。

把三个真实来源确定下来。

为每个来源写一张非常简单的字段表：

| 内部字段            | 来源 A | 来源 B | 来源 C |
|-----------------|------|------|------|
| external_job_id | ?    | ?    | ?    |
| title           | ?    | ?    | ?    |
| company         | ?    | ?    | ?    |
| location        | ?    | ?    | ?    |
| description     | ?    | ?    | ?    |
| salary          | ?    | ?    | ?    |
| experience      | ?    | ?    | ?    |
| education       | ?    | ?    | ?    |
| published_at    | ?    | ?    | ?    |
| source_url      | ?    | ?    | ?    |

你亲自确定：

> 哪些字段真的有。

然后确定最终 `JobDraft`。

### AI 做

根据你的最终字段表：

* 修改 ORM。
* 修改 schema。
* 修改 contract。
* 修改 normalization。
* 删除 mobility。
* 删除旧 Alibaba/Tencent/Jaabz adapter。
* 更新 Source Registry。
* 写 Alembic migration。
* 修受影响测试。

### 当天必须完成

```text
□ 三个真实 source 已确定
□ JobDraft V1 冻结
□ JobPost / Detail V1 冻结
□ 签证/搬迁/无用国际化字段删除
□ 老 Adapter 删除
□ migration 可生成/执行
```

### 不做

> 爬虫实现。

先把后端契约定好。

---

# Day 2 —— 你自己写三个 Crawler

这是非常适合你亲自做的一天。

## 目标

三个真实来源都能够输出：

```text
RawJobCollectedMessage
```

### 你做

自己完成三个小 crawler。

但不要：

```text
crawler → database
```

只能：

```text
crawler
↓
raw payload
↓
message envelope
↓
send_task
```

建议共用一个：

```python
RawJobProducer
```

三个 crawler 只负责：

```text
fetch
parse
build raw payload
```

### AI 做

只帮你：

* 抽取公共 HTTP helper。
* logging。
* CLI 参数。
* 限速/超时。
* producer 公共代码。
* 单元测试。

**不要让 AI 写 crawler selector/parsing 主体。**

### 当天必须完成

至少：

```text
每个来源抓 5～10 条真实岗位
```

能在控制台看到结构正确的数据。

### 不要求

当天不必全部发到 MQ。

---

# Day 3 —— MQ 学习 + 代码所有权转移

这是整个 10 天里最重要的一天之一。

## 今天不追求新功能。

你的目标只有：

> **把现有 MQ 代码真正变成你的。**

### 学习顺序

严格按：

```text
Crawler
↓
Producer
↓
send_task
↓
RabbitMQ
↓
Queue
↓
Celery Worker
↓
Task
↓
ACK
↓
Transaction 1
↓
Transaction 2
↓
Retry
↓
Idempotency
```

### 你做

亲自回答：

1. 为什么 crawler 不 import Worker？
2. 为什么使用 `send_task`？
3. RabbitMQ 在这里保存了什么？
4. `job.import_raw` 是什么？
5. `job.ingestion` 又是什么？
6. `task_routes` 做什么？
7. ACK 是什么时候？
8. Worker ACK 前挂了怎么办？
9. 为什么可能重复消费？
10. `message_id` 防什么？
11. `raw_content_hash` 防什么？
12. `fingerprint` 防什么？
13. 为什么两个事务？
14. Transaction 1 成功，Transaction 2 失败怎么办？
15. 哪些异常 retry？

### AI 做

这一天 AI 的主要身份不是 programmer。

而是：

> **老师 + Reviewer。**

帮你：

* 逐文件讲。
* 画时序。
* 指出现有代码问题。
* 准备修改 patch。
* 出面试题。

### 当天必须完成

你能够不看资料画：

```text
Producer → MQ → Worker → DB
```

并解释整个生命周期。

---

# Day 4 —— MQ 修正 + 三个真实数据源正式接通

今天才修改 MQ。

## 你做

亲自确认三个核心语义：

### A. Identity

冻结：

```text
source_platform
external_job_id / source_url
```

到底谁负责唯一身份。

### B. Idempotency

明确：

```text
message_id
raw content version
fingerprint
```

分别解决什么。

### C. Retry

列一个表：

| 错误                        | Retry?     |
|---------------------------|------------|
| Pydantic contract invalid | No         |
| unsupported source        | No         |
| DB connection lost        | Yes        |
| deadlock                  | Yes        |
| Worker crash              | redelivery |
| LLM timeout               | 后面 Yes     |
| LLM 429                   | 后面 Yes     |

### AI 做

根据你确认的规则：

* 修 envelope / adapter 信息重复。
* 修 raw hash identity。
* 清理旧 TODO。
* 修 retry classifier。
* 更新 MQ tests。
* 更新 integration doc。

### 实验

必须真实做：

```text
① 同 message_id 两次
② 不同 message_id，同一个岗位相同内容
③ 同岗位内容更新
④ Worker kill
⑤ 数据库临时断开
⑥ 非法 message
```

### 当天结束

三个 crawler：

```text
Crawler A ─┐
Crawler B ─┼→ RabbitMQ → JobPost
Crawler C ─┘
```

至少真实跑通。

---

# Day 5 —— LLM SkillExtractor

今天才接 AI。

## 你先设计

先不要写 API 调用。

你定义：

```python
class RawSkillCandidate:
    name: str
    evidence: str | None
```

或者你认为需要的最小字段。

我倾向于 v1.0 只要：

```text
skill_text
evidence_text
```

够了。

不建议模型输出：

```text
skill_id
required_level
importance
proficiency
confidence
```

先别搞。

### 核心原则

```text
LLM
负责：
自然语言 → 技能候选

SkillAlias
负责：
技能候选 → Canonical Skill
```

### 你做

准备 10～20 条真实 JD。

人工写一份：

> 你认为应该抽出来什么技能。

它就是最简单的 evaluation set。

### AI 做

实现：

```text
SkillExtractor Protocol
LLMSkillExtractor
FakeSkillExtractor
```

以及：

* structured output。
* timeout。
* retryable error。
* response validation。
* 日志。
* 测试。

### 当天完成

```text
真实 JD
↓
LLM
↓
RawSkillCandidate
↓
SkillAlias
↓
JobPostSkill
```

跑通。

---

# Day 6 —— LLM + MQ 两阶段事务完整闭环

Day 5 是 LLM 单体。

Day 6 把它真正接入：

```text
job.import_raw
```

## Transaction 1

```text
RawJobRecord
JobPost
JobPostDetail
COMMIT
```

## Transaction 2

```text
Reload RawJob
↓
SkillExtractor
↓
SkillAlias
↓
JobPostSkill
COMMIT
```

### 你做

重点理解：

> 为什么不是一个事务？

并亲自做：

```text
LLM 故意 timeout
```

验证：

> JobPost 仍然存在。

然后再次执行：

> Skill enrichment 可以恢复。

### AI 做

* 重构 recovery service。
* 接 SkillExtractor。
* 修改 Skill Sync status。
* 增加 LLM 错误分类。
* 测试。

### 当天 Release Gate

```text
□ 三来源真实数据入库
□ LLM 技能抽取
□ Alias normalization
□ LLM 挂掉岗位不会丢
□ Retry 后技能可恢复
```

到这里：

# 数据入口正式完成。

---

# Day 7 —— Auth UserSession

现在处理你最开始担心的认证。

不要碰 OAuth。

## 最终体系

```text
User
↓
AuthIdentity
↓
PasswordCredential

User
↓
UserSession
```

Session 至少保存：

```text
id / session_jti
user_id
refresh_token_jti/hash 或 rotation state
device_name
created_at
last_seen_at
expires_at
revoked_at
```

具体字段 Day 7 再设计。

### 功能只做

```text
Login → 新 session

Refresh
→ 校验 session
→ rotation

Current logout
→ revoke 当前 session

Logout all
→ revoke 用户所有 session

GET sessions
→ 查看登录设备
```

如果还有时间：

```text
revoke specific session
```

### Access blacklist

这里不要为了简历硬做复杂方案。

如果当前 session 每次鉴权已经可以查询 Redis/DB session state：

> 可以直接通过 session revoked 判断。

是否单独 blacklist access jti，根据最终设计决定。

### 你做

必须亲自设计：

> Session 为什么存在？

并能解释：

```text
JWT 无状态
vs
服务端 Session state
```

### AI 做

* ORM。
* migration。
* repository。
* API。
* token claim。
* tests。

### 不做

```text
短信
验证码
OAuth
复杂 RBAC
Admin
```

---

# Day 8 —— 用户业务闭环联调，不重写

这一天不要增加大业务。

拿已有：

```text
Target
UserSkill
JobMatch
StudyTask
Question
```

做一次彻底联调。

## 目标流程

用户：

```text
注册
↓
选择目标岗位
↓
录入 Python=4
Redis=1
↓
Match
```

得到：

```text
Python → matched
Redis → weak
RabbitMQ → missing
```

然后：

```text
Generate StudyTask
```

生成：

```text
Redis 学习任务
RabbitMQ 学习任务
```

用户答题：

```text
progress 更新
```

结束。

### 你做

重点检查业务规则。

尤其：

> **不要自动提升 UserSkill proficiency。**

当前 AGENTS 里还有：

> “作答后按规则调整用户技能评级”

我建议 v1.0 删除这句话。

因为现在没有可靠规则。

最终变成：

> 学习任务只记录学习进度；UserSkill 由用户主动维护。

然后用户改：

```text
Redis 1 → 3
```

再次 Match：

```text
weak → matched
```

这就已经是完整反馈闭环。

### AI 做

* 删除不可信自动评级逻辑/文档。
* 修 service。
* 补 integration tests。
* 统一接口响应。

---

# Day 9 —— Docker + Nginx + End-to-End Demo

这里把你之前 Session 2 学过的东西正式落进项目。

最终 Compose：

```text
nginx
api
worker
postgres
redis
rabbitmq
```

Simulator / Crawler 可以：

> 本地独立运行。

因为这样更能证明 Producer 独立。

### 你做

亲自配置/理解：

```text
Nginx
↓
FastAPI
```

并做：

```text
正常请求
502
504
```

你之前刚学这一块，正好融合项目。

### AI 做

* Docker multi-stage 或现有 Dockerfile 优化。
* Compose services。
* healthcheck。
* depends_on。
* Worker command。
* Nginx config。
* env example。
* PowerShell demo script。

### 最终要求

新人 clone：

```bash
docker compose up -d
```

核心环境能够启动。

---

# Day 10 —— 作品化 + 简历 + 面试资产

最后一天禁止新增功能。

## 第一件事：README 重写

README 不再出现：

> “围绕简历补代码”

> “简历目标优先”

> “阶段 7 后续完善”

这种开发者自言自语。

最终结构：

```text
# JobPilot

一句话介绍

## Architecture
架构图

## Core Workflow
真实岗位摄入链路

## Key Features

## Engineering Decisions
- 为什么 Adapter
- 为什么 RabbitMQ
- 为什么两阶段事务
- 为什么三层幂等
- 为什么 LLM + Alias
- 为什么 JWT + UserSession

## Quick Start

## Demo

## Testing

## Project Structure

## Current Limitations
```

---

## 第二件事：准备项目 Demo

最好一个：

```powershell
demo.ps1
```

能够展示：

```text
启动
↓
发岗位
↓
Worker 消费
↓
LLM 抽技能
↓
查询 JobPost
↓
登录用户
↓
设置 Target
↓
设置 UserSkill
↓
Match
↓
Generate StudyTask
```

这比前端有价值得多。

---

## 第三件事：简历 v1.0

这时候才正式重写。

不是：

> 根据想做什么写简历。

而是：

> 根据 GitHub 当前真实代码提炼。

---

## 第四件事：项目追问题库

至少准备六组：

### Auth

```text
为什么 JWT？
为什么 refresh token？
为什么 UserSession？
JWT 被盗怎么办？
```

### MQ

```text
为什么 MQ？
为什么 RabbitMQ？
ACK 怎么处理？
重复消费怎么办？
```

### Idempotency

```text
message_id / raw hash / fingerprint 区别？
```

### Transaction

```text
为什么两阶段？
如果第二阶段失败？
```

### AI

```text
为什么 LLM？
为什么不用传统 NER？
为什么还需要 Alias？
模型乱输出怎么办？
```

### Database

```text
表为什么这么拆？
有哪些唯一约束？
为什么这些索引？
```

---

# 七、10 天时间分配其实不是平均的

整个项目大概是：

```text
Day 1
数据模型收缩

Day 2
Crawler

Day 3-4
MQ

Day 5-6
LLM + Skill

Day 7
Session

Day 8
业务闭环

Day 9
部署

Day 10
作品 / 简历
```

对应技术比重：

| 模块                | 本轮投入 |
|-------------------|-----:|
| MQ + ingestion    |  25% |
| LLM Skill         |  20% |
| Crawler + Adapter |  15% |
| Auth Session      |  15% |
| 用户闭环              |  10% |
| Deployment        |  10% |
| README/简历         |   5% |

---

# 八、算法和八股这 10 天怎么处理

这 10 天我反而不建议你同时全面学习八股。

## 算法

每天只维持：

> **20～40 分钟。**

JobPilot 主任务日：

```text
旧题盲写 1 道
```

有精神：

```text
二分新题 1 道
```

这 10 天只把：

> **二分**

做掉就行。

不要急着栈、堆、图。

---

## 八股

几乎全部绑定 JobPilot。

例如：

Day 3：

> MQ 八股。

Day 5：

> LLM structured output / timeout。

Day 7：

> JWT / Session。

Day 9：

> Nginx / Docker。

这样你的实际学习是：

```text
项目 ≈ 八股
```

不会产生三个并行大任务。

---

# 九、10 天之后的 JobPilot v1.0 Definition of Done

只有下面这些全部达到，才叫完成。

## 数据

* [ ] 三个真实国内数据源。
* [ ] 每个来源 Adapter。
* [ ] 真实 JobPost 数据。
* [ ] 无无用签证/搬迁等国际化模型。

## MQ

* [ ] Producer 与 Backend 解耦。
* [ ] RabbitMQ → Worker 跑通。
* [ ] late ACK。
* [ ] retry 分类。
* [ ] message_id 幂等。
* [ ] raw version 幂等。
* [ ] fingerprint 去重。
* [ ] Worker crash 实验。
* [ ] 两阶段事务。

## AI

* [ ] LLM SkillExtractor。
* [ ] structured output。
* [ ] SkillAlias normalization。
* [ ] JobPostSkill。
* [ ] timeout/retry。
* [ ] LLM 失败不丢 JobPost。

## Auth

* [ ] password login。
* [ ] access + refresh。
* [ ] UserSession。
* [ ] refresh rotation。
* [ ] 当前设备退出。
* [ ] 全部设备退出。
* [ ] sessions 查询。

## 用户业务

* [ ] Target。
* [ ] UserSkill。
* [ ] matched / weak / missing。
* [ ] StudyTask generation。
* [ ] question progress。
* [ ] 不做虚假的自动技能评级。

## 工程

* [ ] PostgreSQL。
* [ ] Redis。
* [ ] RabbitMQ。
* [ ] Celery。
* [ ] Docker Compose。
* [ ] Nginx。
* [ ] `/health`。
* [ ] `/health/readiness`。
* [ ] CI。
* [ ] 核心测试全部通过。

## 作品

* [ ] README。
* [ ] 架构图。
* [ ] Quick Start。
* [ ] Demo Script。
* [ ] 简历与代码完全一致。
* [ ] 你能 5 分钟讲完整项目。
* [ ] 关键技术能够接受 3～5 层追问。

---

# 十、最后再给你划一道非常重要的边界

10 天结束的时候，即使还有：

```text
UI 没有
OAuth 没有
管理员没有
ES 没有
K8s 没有
RAG 没有
Agent 没有
监控平台没有
推荐算法很简单
学习规划不智能
```

**JobPilot 也必须宣布 v1.0 完成。**

因为这时候它已经能证明：

> 你能设计一个模块化 Python 后端；
> 能处理真实异构数据；
> 能利用 Adapter 解耦数据源；
> 能用 RabbitMQ/Celery 做可靠异步处理；
> 理解幂等、ACK、Retry、事务恢复；
> 能把 LLM 用在真实业务的数据结构化问题上；
> 能用 Skill Dictionary 做确定性治理；
> 有认证、用户隔离和业务闭环；
> 能容器化运行和测试。

这已经远比“功能特别多但是自己说不清楚”的项目更适合秋招。

**这 10 天真正的主题不是开发更多功能，而是把 JobPilot 的所有权从 AI 手里拿回来。**第 10 天完成后，你应该能明确说：哪些东西是我设计的、为什么这么设计、AI
帮我完成了哪些机械实现、我又如何测试证明它是正确的。这个叙事本身甚至与你刚才看到的 AI-Native 后端岗位非常契合。
