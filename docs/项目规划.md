# 一、JobPilot 简历目标版

**2026.04 - 2026.07（持续开发中） JobPilot 招聘岗位情报与求职准备平台 Python后端开发**

* **项目描述：** 基于 **FastAPI 模块化后端架构**
  开发的招聘岗位情报与求职准备平台，围绕岗位数据摄入、清洗去重、技能标签提取、岗位搜索筛选、目标岗位管理、技能差距分析、学习任务生成、八股题掌握记录等场景，形成
  **“岗位情报 → 技能分析 → 学习准备”** 的后端业务闭环。系统重点体现 **认证鉴权、数据建模、复杂查询、缓存优化、异步任务、幂等处理、测试与容器化部署
  ** 等后端工程能力。
* **技术栈：** **FastAPI、Pydantic v2、SQLAlchemy 2.0、Alembic、PostgreSQL、Redis、Celery、JWT、Docker Compose、pytest、uv**
* **个人职责：**

    1. 基于 **FastAPI + SQLAlchemy 2.0** 完成后端模块化设计，将系统拆分为 **用户认证、岗位数据、数据摄入、用户工作台、学习准备、缓存与异步任务
       ** 等领域模块，采用 **router / schema / service / repository / model** 分层组织代码。
    2. 设计 **JWT access token + refresh token** 认证体系，支持用户注册登录、token 刷新、会话撤销、用户禁用校验；通过 *
       *FastAPI Depends** 获取当前用户，并在收藏、目标岗位、技能画像、学习任务等模块中实现 **用户数据隔离**。
    3. 设计 **岗位数据模型与技能标签模型**，支持岗位列表、详情、关键词搜索、城市/薪资/技能多条件筛选；通过 **岗位
       fingerprint + 数据库唯一约束** 实现岗位去重，使用 **Alembic** 管理数据库结构演进。
    4. 设计 **目标岗位与用户技能画像模块**，支持用户收藏岗位、设为目标岗位、维护个人技能水平；基于岗位技能与用户技能进行 *
       *matched / missing / weak 技能差距分析**，为学习任务生成提供依据。
    5. 设计 **学习任务与八股题掌握模块**，支持根据目标岗位缺失技能生成学习任务，按技能推荐面试题，并记录用户对题目的 *
       *todo / reviewing / mastered** 掌握状态，形成岗位准备闭环。
    6. 引入 **Redis Cache Aside 缓存模式**，缓存岗位详情、热门技能统计、任务进度等高频数据；在写操作后删除相关缓存，降低数据库重复查询压力，并保证
       **数据库作为最终事实来源**。
    7. 使用 **Celery + Redis** 处理岗位数据摄入、字段清洗、技能提取、去重入库等异步任务，避免耗时任务阻塞 HTTP 请求；结合 *
       *任务状态表、错误记录、唯一约束和幂等设计** 处理重复执行、部分失败和失败重试等场景。
    8. 编写 **pytest 单元测试与接口测试**，覆盖认证鉴权、权限隔离、岗位查询、收藏目标岗位、技能差距分析、学习任务生成、数据摄入去重等核心流程；使用
       **Docker Compose** 编排 PostgreSQL、Redis、API、Worker 等服务，保证项目可运行、可迁移、可测试。
* **技术亮点：** 使用 **Redis、Celery、唯一约束、幂等任务、Cache Aside、用户数据隔离、异步数据摄入**
  保障系统在高频查询和批量数据处理场景下的稳定性与可扩展性。

---

# 三、领域 1：用户与认证域

| 分类    | 设计内容                                                                                                                                        |
|-------|---------------------------------------------------------------------------------------------------------------------------------------------|
| 对应模块  | `auth`、`users`、`auth_accounts`、`user_sessions`                                                                                              |
| 业务目标  | 解决“用户是谁、是否登录、是否可用、数据归谁”的问题                                                                                                                  |
| 核心功能  | 注册、登录、刷新 token、退出登录、获取当前用户、禁用用户校验                                                                                                           |
| 核心表   | `users`、`auth_accounts`、`user_sessions`                                                                                                     |
| 技术栈   | **FastAPI Depends、JWT、Refresh Token、Password Hash、SQLAlchemy、PostgreSQL、Pydantic、pytest**                                                   |
| 简历亮点  | **access token + refresh token 双 token 机制**；refresh token 落库；支持 logout 和 session 撤销；用户禁用后接口访问受限；密码哈希存储；通过依赖注入获取当前用户                         |
| 工程难点  | JWT 本身无状态，退出登录不能简单让 access token 失效；refresh token 落库后才能支持撤销、设备管理、禁用用户等行为                                                                    |
| 测试点   | 注册成功、邮箱重复、弱密码、登录成功、密码错误、token 失效、refresh 成功、logout 后 refresh 失败、禁用用户访问失败                                                                    |
| 可引导八股 | **JWT 原理是什么？JWT 有哪些优缺点？access token 和 refresh token 为什么要分开？JWT 如何实现退出登录？refresh token 为什么要落库？密码为什么不能明文存？鉴权和授权有什么区别？FastAPI Depends 有什么作用？** |
| 经典追问  | 用户退出登录后 access token 仍然有效怎么办？如何只退出当前设备？如何踢掉某个用户所有登录态？用户被禁用后已经签发的 token 怎么处理？                                                                |
| 面试讲法  | “access token 保持短有效期，主接口鉴权不查库；refresh token 落库保存 hash，用于刷新和撤销。logout 本质上是撤销 refresh session，而不是让所有 JWT 立刻失效。”                               |

---

# 四、领域 2：岗位数据域

| 分类    | 设计内容                                                                                                                |
|-------|---------------------------------------------------------------------------------------------------------------------|
| 对应模块  | `job_posts`、`job_skills`、`job_sources`                                                                              |
| 业务目标  | 维护系统内部的岗位情报数据，支持查询、筛选、去重和技能标签展示                                                                                     |
| 核心功能  | 岗位列表、岗位详情、关键词搜索、城市筛选、薪资筛选、技能筛选、热门技能统计、岗位来源链接跳转                                                                      |
| 核心表   | `job_posts`、`job_skills`、`job_sources`                                                                              |
| 技术栈   | **PostgreSQL、SQLAlchemy 2.0、Alembic、动态查询、分页、索引、唯一约束**                                                               |
| 简历亮点  | 岗位数据不由普通用户创建，而是来自系统数据源；通过 **fingerprint + 唯一约束** 去重；支持多条件筛选；技能标签从 JD 中提取；热门技能统计可缓存                                  |
| 工程难点  | 岗位来源可能重复；岗位标题、公司、薪资、城市格式可能不统一；多条件筛选容易导致 SQL 复杂和索引失效                                                                 |
| 测试点   | 创建岗位、重复岗位去重、分页查询、关键词查询、城市筛选、薪资筛选、技能筛选、岗位详情不存在                                                                       |
| 可引导八股 | **唯一约束和普通索引有什么区别？为什么去重不能只靠代码？联合索引怎么设计？最左前缀原则是什么？limit offset 深分页有什么问题？LIKE 查询为什么可能不走索引？覆盖索引是什么？回表是什么？SQL 慢查询怎么排查？** |
| 经典追问  | 同一个岗位多个平台都采集到了怎么去重？岗位列表查询越来越慢怎么优化？按技能筛选岗位应该用 JSON 字段还是单独拆表？岗位详情适合缓存吗？                                               |
| 面试讲法  | “岗位去重不能只放在 service 层判断，因为并发下可能重复插入，所以用 fingerprint 生成唯一业务指纹，并在数据库层加唯一约束兜底。”                                         |

---

# 五、领域 3：数据摄入与清洗域

注意：这里不是“用户上传 CSV”，而是**系统级岗位数据摄入管道**。

| 分类    | 设计内容                                                                                                                           |
|-------|--------------------------------------------------------------------------------------------------------------------------------|
| 对应模块  | `ingestion_tasks`、`raw_job_records`、`ingestion_errors`、`job_normalization`、`deduplication`                                     |
| 业务目标  | 将外部岗位数据源进入系统，完成清洗、校验、去重、技能提取和入库                                                                                                |
| 核心功能  | 创建摄入任务、读取 seed/JSON/爬虫输出、字段校验、薪资解析、岗位去重、技能提取、错误记录、任务状态查询                                                                       |
| 核心表   | `ingestion_tasks`、`raw_job_records`、`ingestion_errors`                                                                         |
| 技术栈   | **Celery、Redis、PostgreSQL、批量入库、幂等设计、任务状态机、错误记录**                                                                               |
| 简历亮点  | 数据摄入不阻塞 HTTP；后台 Worker 处理清洗和入库；合法数据写入岗位表；错误数据写入错误表；Redis 记录任务进度；PostgreSQL 保存最终状态                                              |
| 工程难点  | 任务可能重复执行；数据可能部分成功部分失败；字段格式混乱；Worker 可能中途挂掉；重复岗位不能重复入库                                                                          |
| 任务状态  | `pending`、`processing`、`success`、`partial_failed`、`failed`                                                                     |
| 测试点   | 创建任务、正常入库、重复入库不重复、非法数据记录错误、部分失败状态、重复执行幂等                                                                                       |
| 可引导八股 | **为什么耗时任务不能放在 HTTP 请求里同步执行？Celery 的 broker 和 backend 分别是什么？Redis 能不能做消息队列？什么是幂等？任务重复消费怎么办？任务失败如何重试？数据库事务边界怎么设计？批量入库怎么优化？**     |
| 经典追问  | Worker 处理到一半挂了怎么办？同一个任务被重复执行怎么办？任务状态应该放 Redis 还是数据库？部分成功部分失败如何设计状态？如何保证数据不重复？                                                  |
| 面试讲法  | “HTTP 接口只负责创建 ingestion task 并返回 task_id，Worker 后台处理数据。最终状态落 PostgreSQL，实时进度放 Redis。重复执行时依赖 task 状态、岗位 fingerprint 和唯一约束保证幂等。” |

---

# 六、领域 4：用户工作台域

| 分类     | 设计内容                                                                                                     |
|--------|----------------------------------------------------------------------------------------------------------|
| 对应模块   | `job_collections`、`job_targets`、`user_skills`、`job_match`                                                |
| 业务目标   | 用户围绕岗位建立自己的准备目标，而不是在系统内完成投递                                                                              |
| 核心功能   | 收藏岗位、取消收藏、设为目标岗位、设置目标优先级、维护用户技能画像、岗位技能差距分析                                                               |
| 核心表    | `job_collections`、`job_targets`、`user_skills`                                                            |
| 技术栈    | **PostgreSQL、SQLAlchemy relationship、唯一约束、用户数据隔离、枚举状态、Pydantic schema**                                  |
| 简历亮点   | 收藏表示感兴趣，目标岗位表示准备对象；用户维护个人技能画像；系统对比岗位技能和用户技能，生成 matched/missing/weak 分析结果                                 |
| 工程难点   | 用户数据隔离；重复收藏；目标岗位状态管理；岗位下架后用户目标如何处理；技能匹配逻辑如何和数据模型解耦                                                       |
| 目标岗位状态 | `active`、`paused`、`completed`、`abandoned`                                                                |
| 用户技能等级 | `unknown`、`basic`、`intermediate`、`advanced`                                                              |
| 测试点    | 收藏岗位、重复收藏、取消收藏、设为目标岗位、重复目标岗位、A 用户不能访问 B 用户目标、技能匹配结果正确                                                    |
| 可引导八股  | **一对多、多对多关系怎么设计？为什么收藏表需要 user_id + job_id 唯一约束？如何防止越权访问？业务状态用字符串、整数还是枚举？Service 层和 Repository 层分别负责什么？** |
| 经典追问   | 用户重复收藏同一个岗位怎么办？用户删除目标岗位，学习任务是否删除？岗位被删除后收藏记录如何处理？A 用户传 B 用户的 target_id，如何防止越权？                            |
| 面试讲法   | “收藏和目标岗位不是一个概念，所以拆成两个表。收藏是轻量兴趣记录，目标岗位是学习准备的业务入口，后续学习任务会关联 job_target。”                                   |

---

# 七、领域 5：学习准备域

| 分类     | 设计内容                                                                                                 |
|--------|------------------------------------------------------------------------------------------------------|
| 对应模块   | `study_tasks`、`knowledge_points`、`interview_questions`、`question_mastery_records`、`practice_records` |
| 业务目标   | 根据目标岗位缺失技能，生成学习任务和题目练习，记录用户掌握情况                                                                      |
| 核心功能   | 创建学习任务、从缺失技能生成任务、题库查询、按技能推荐题目、记录题目掌握状态、练习记录                                                          |
| 核心表    | `study_tasks`、`knowledge_points`、`interview_questions`、`question_mastery_records`、`practice_records` |
| 技术栈    | **PostgreSQL、SQLAlchemy、枚举状态、聚合查询、pytest**                                                           |
| 简历亮点   | 不是单纯题库 CRUD，而是和目标岗位、缺失技能关联；题目是公共资源，掌握状态是用户私有记录                                                       |
| 工程难点   | 公共题库和用户掌握状态分离；任务状态流转；学习任务如何关联目标岗位；已掌握题目是否继续推荐                                                        |
| 学习任务状态 | `todo`、`doing`、`done`、`cancelled`                                                                    |
| 题目掌握状态 | `todo`、`reviewing`、`mastered`                                                                        |
| 测试点    | 创建学习任务、从缺失技能生成任务、查询我的任务、更新任务状态、推荐题目、记录题目掌握状态                                                         |
| 可引导八股  | **任务状态流转如何设计？公共数据和用户私有数据如何拆表？一条题目如何记录不同用户的掌握状态？如何设计题库和练习记录？如何写聚合统计 SQL？**                            |
| 经典追问   | 同一道题多个用户都练习怎么设计？用户已掌握的题目还要不要推荐？学习任务如何排序？任务删除是物理删除还是逻辑删除？                                             |
| 面试讲法   | “题库是公共资源，不能把掌握状态直接放在题目表里，所以用 question_mastery_records 单独记录 user_id、question_id 和掌握状态。”               |

---

# 八、领域 6：缓存与性能域

| 分类    | 设计内容                                                                                                                  |
|-------|-----------------------------------------------------------------------------------------------------------------------|
| 对应模块  | `cache`、`job_detail_cache`、`hot_skill_cache`、`match_result_cache`、`task_progress_cache`                               |
| 业务目标  | 优化高频读接口和任务进度查询，减少数据库压力                                                                                                |
| 核心功能  | 岗位详情缓存、热门技能统计缓存、岗位匹配结果缓存、任务进度缓存、缓存删除                                                                                  |
| 技术栈   | **Redis、Cache Aside、TTL、缓存失效、分布式锁可后期**                                                                                |
| 简历亮点  | 使用 Cache Aside 模式；写操作后删除相关缓存；数据库仍作为最终事实来源；任务进度放 Redis 提高轮询效率                                                          |
| 工程难点  | 缓存和数据库一致性；缓存穿透；缓存击穿；缓存雪崩；热点 key；大 key                                                                                 |
| 测试点   | cache miss、cache hit、写后删缓存、空值缓存、TTL、生效和失效                                                                             |
| 可引导八股 | **Redis 为什么快？Redis 常见数据类型有哪些？Cache Aside 是什么？为什么写操作通常删除缓存而不是更新缓存？缓存穿透、击穿、雪崩分别怎么解决？Redis 和数据库如何保证一致性？Redis 分布式锁怎么实现？** |
| 经典追问  | 热门岗位被频繁访问怎么优化？岗位更新后缓存如何失效？大量请求不存在岗位 ID 怎么防穿透？缓存重建时大量请求同时打数据库怎么办？                                                      |
| 面试讲法  | “读接口先查 Redis，miss 后查数据库并回填；写操作以数据库为准，提交成功后删除缓存。缓存只是加速层，不作为事实来源。”                                                      |

---

# 九、领域 7：异步任务与协调域

| 分类    | 设计内容                                                                                         |
|-------|----------------------------------------------------------------------------------------------|
| 对应模块  | `celery_app`、`ingestion_worker`、`skill_extract_worker`、`async_tasks`                         |
| 业务目标  | 把耗时、可重试、不适合阻塞 HTTP 的任务交给后台 Worker 处理                                                         |
| 核心功能  | 数据摄入任务、技能提取任务、热门统计刷新、任务重试、任务状态查询                                                             |
| 技术栈   | **Celery、Redis/RabbitMQ Broker、Redis Result Backend、Flower、任务重试、幂等、任务状态表**                   |
| 简历亮点  | HTTP 接口只创建任务并返回 task_id；Worker 后台处理；任务状态持久化；失败记录错误原因；通过幂等设计避免重复处理                            |
| 工程难点  | 消息丢失、重复消费、任务失败、Worker 崩溃、并发执行同一任务、任务状态不一致                                                    |
| 测试点   | 任务创建、任务执行、任务失败、重复执行、状态更新、错误记录                                                                |
| 可引导八股 | **消息队列有什么作用？什么是异步解耦？什么是削峰填谷？Celery ack 是什么？任务重复消费怎么办？死信队列是什么？Outbox Pattern 解决什么问题？幂等如何设计？** |
| 经典追问  | 接口写数据库成功但消息发送失败怎么办？任务执行成功但状态更新失败怎么办？Worker 多实例并发消费如何避免重复处理？任务失败重试会不会导致重复入库？                  |
| 面试讲法  | “异步任务必须默认可能重复执行，所以业务处理不能依赖‘只执行一次’。岗位入库用 fingerprint 唯一约束兜底，任务执行前后更新状态，并记录错误明细。”              |

---

# 十、领域 8：测试、部署与工程化域

| 分类    | 设计内容                                                                                                                               |
|-------|------------------------------------------------------------------------------------------------------------------------------------|
| 对应模块  | `tests`、`Dockerfile`、`docker-compose.yml`、`alembic`、`health_check`、`logging`、`README`                                              |
| 业务目标  | 保证项目可运行、可迁移、可测试、可复盘                                                                                                                |
| 核心功能  | 单元测试、接口测试、测试数据库隔离、数据库迁移、Docker Compose 启动、健康检查、日志                                                                                  |
| 技术栈   | **pytest、pytest-asyncio、httpx、Alembic、Docker Compose、ruff、pyright、uv**                                                             |
| 简历亮点  | 测试覆盖认证、权限隔离、岗位查询、收藏目标岗位、技能差距分析、学习任务生成、数据摄入去重；开发库和测试库隔离；Docker Compose 一键启动                                                         |
| 工程难点  | 测试污染数据库；异步接口测试；数据库迁移一致性；本地环境复现；配置管理                                                                                                |
| 测试点   | auth 测试、权限测试、jobs 查询测试、收藏目标测试、match service 测试、study task 测试、ingestion 测试                                                          |
| 可引导八股 | **单元测试和集成测试区别是什么？为什么测试数据库要和开发数据库隔离？Alembic migration 有什么作用？Docker 和虚拟机有什么区别？Docker Compose 解决什么问题？健康检查为什么分 liveness 和 readiness？** |
| 经典追问  | 本地能跑，别人电脑跑不起来怎么办？数据库字段变更如何发布？如何回滚 migration？测试如何避免污染真实数据库？                                                                         |
| 面试讲法  | “测试默认使用独立测试数据库，避免污染开发数据。数据库结构变更通过 Alembic migration 管理，Docker Compose 统一启动 PostgreSQL、Redis、API 和 Worker。”                         |

---

# 十一、实现顺序

| 阶段 | 实现内容                                    | 对应简历能力                 |
|----|-----------------------------------------|------------------------|
| 0  | auth/users/session                      | JWT、refresh token、用户隔离 |
| 1  | job_posts/job_skills                    | 数据建模、搜索筛选、唯一约束         |
| 2  | job_collections/job_targets/user_skills | 用户工作台、权限隔离             |
| 3  | job_match                               | 技能差距分析、业务 service      |
| 4  | study_tasks/questions/mastery           | 学习闭环、公共题库与用户状态分离       |
| 5  | Redis 缓存                                | Cache Aside、缓存一致性      |
| 6  | Celery ingestion                        | 异步任务、幂等、错误记录           |
| 7  | pytest + Docker + README                | 工程化、可运行、可测试            |

---

# 十二、最重要的实现原则

以后每做一个模块，都要同时产出 4 个东西：

```text
1. 代码
2. 测试
3. README / 学习文档
4. 涉及八股问题
```
