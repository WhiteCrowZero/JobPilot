[project]
name = "jobpilot-crawler-simulator"
version = "0.1.0"
description = "Standalone RabbitMQ producer for JobPilot ingestion integration"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.3",
]

[tool.uv]
package = false
